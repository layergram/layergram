{
  "schemaVersion": 1,
  "status": "candidate-not-production-approved",
  "repository": "https://github.com/pq-code-package/mlkem-native.git",
  "release": "v2.0.0",
  "commit": "d1b2fe782888bdb761a50336012923180be7f502",
  "parameterSet": "ML-KEM-768",
  "licenseChoiceForLibrarySources": "Apache-2.0",
  "observedOn": "2026-08-13",
  "observedPlatform": "macOS arm64 host; Android 14 arm64 emulator; iOS arm64 simulator; Windows 11 ARM64 host running Flutter windows-x64",
  "observedChecks": [
    "upstream make build",
    "upstream functional tests",
    "upstream KAT verification",
    "upstream ACVP vectors v1.1.0.43",
    "upstream Wycheproof vectors",
    "upstream unit, allocation, RNG-failure, and ABI checks",
    "Layergram C ABI warning-as-error build on macOS arm64",
    "Layergram native KAT, negative, implicit-rejection, lifecycle, OS-CSPRNG, and zeroization tests",
    "Layergram Dart FFI KAT and production-library traversal",
    "Layergram native AddressSanitizer and UndefinedBehaviorSanitizer run",
    "production export surface excludes upstream and deterministic test symbols",
    "Android debug and release APK packaging for armeabi-v7a, arm64-v8a, and x86_64",
    "Android 14 arm64 emulator packaged production-ABI traversal",
    "iOS arm64/x86_64 simulator build and arm64 simulator packaged production-ABI traversal",
    "unsigned iOS arm64 release build with production symbols retained after dead stripping",
    "macOS arm64/x86_64 embedded framework build and unsigned app production-ABI traversal",
    "Windows x64 release DLL exact production export surface and BCrypt dependency",
    "Windows x64 packaged Flutter integration and release-process traversal on Windows 11 ARM64",
    "Windows x64 native negative, implicit-rejection, CSPRNG, lifecycle, and zeroization tests",
    "Windows Store-mode MSIX contains the production ML-KEM DLL",
    "vendored mlkem tree and fixtures match pinned upstream Git objects"
  ],
  "remainingGates": [
    "Android physical-device and store-signing tests",
    "iOS physical-device and distribution-signing tests",
    "macOS distribution signing and notarization",
    "native Windows arm64 build if distributed",
    "Linux x64 and arm64 builds where distributed",
    "full ACVP and Wycheproof traversal through each shipped wrapper ABI",
    "complete supply-chain and packaged-license inventory",
    "independent implementation audit",
    "incremental API decision for sparse PQ ratchet"
  ]
}
